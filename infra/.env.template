DOMAIN=gealgeolgeo.ditpps.com
LETSENCRYPT_EMAIL=admin@example.com

# Path di host ke file service account GEE.
# Default ini menunjuk ke root repo: ./gee-service-account-key.json
GEE_KEY_FILE_HOST=../gee-service-account-key.json

# Optional build args (untuk frontend Vite saat image build)
# Bisa dikosongkan jika sudah diset di .env.example
BUILD_VITE_API_URL=
BUILD_VITE_SUPABASE_URL=
BUILD_VITE_SUPABASE_ANON_KEY=
